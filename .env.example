# Rename this file to .env and add your OpenAI API key
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-..."

# Optional: Specify a different model (defaults to gpt-4o-mini if not set)
OPENAI_MODEL="gpt-4o-mini"

# Optional: Specify a custom base URL for OpenAI compatible APIs (e.g., local LLM server)
# OPENAI_BASE_URL="http://localhost:8000/v1"